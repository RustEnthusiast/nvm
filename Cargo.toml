[package]
name = "nvm"
version = "0.0.0"
edition = "2021"

[[bin]]
name = "nvm"
path = "src/bin/main.rs"
required-features = ["bin"]

[features]
bin = ["thiserror"]

[dependencies]
thiserror = { version = "1.0", optional = true }
